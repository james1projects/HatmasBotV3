# Handoff prompt: next session (timed messages, then "make Hats matter")

Paste everything below the line into a fresh session in C:\Projects\HatmasBot.

---

You are continuing HatmasBot work with James. Read `docs/HATMASBOT.md`
"Working in this repo" first (file-tool desync rules: verify edits from
bash, `python tools/run_tests.py` is the only trustworthy verifier), then
the two newest sections at the end of that file: "v2.16 Update — One uuid
per viewer" and "v2.17 Update — The wallet". Memory files in
`~/.claude/projects/C--Projects-HatmasBot/memory/` (`user-identity-2026-09-12`,
`wallet-2026-09-13`, `generalize-someday-2026-09-12`) hold the decisions.

State on 2026-09-13: every viewer is a `user_uuid` (`core/users.py`,
never `user_id`); Hats and God Tokens live in `core/wallet.py` (every
write needs a `reason` from `wallet.REASONS`, pass a `ref` for anything
event-driven); MixItUp is no longer read by any plugin, the import ran
(20.9M hats, 14.9M watch minutes), and James keeps MixItUp installed only
to discover what else still lives there. Passive earning is 25 Hats per
5-minute tick while live (`plugins/wallet.py`). Nothing has been live-
tested yet: James streams next weekend; after that stream run
`tools/wallet_audit.py` and read the `wallet_earn_ticks` rows.

Tooling notes for this environment: the Bash tool mangles single quotes
and backticks inside heredocs and double-quoted commands, so write patch
scripts and files with the Write tool and run them. The Python to use is
`C:\Users\james\AppData\Local\Programs\Python\Python314\python.exe`
(plain `python` is the Store stub). Delegate bulk generation to the
`local-workers` MCP fleet where it pays, review everything it returns.

## Task 1: timed chat messages, managed from hatmaster.tv/mod

This is the last known MixItUp feature: messages the bot says in chat on
a rotation. Build it so James can move his existing MixItUp timers over
by hand. Requirements he gave:

- Managed on the existing `/mod` page (`core/public_webserver.py`
  `_handle_mod_page` + `/api/mod/*`, gated by `_mod_identity`; custom
  commands already live there via `plugins/custom_commands.py` and
  `data/custom_commands.json` - copy that shape).
- Each message belongs to a **lane** (rotation group), the same idea as
  the alert box's lanes (`core/alert_box.py`, `docs/ALERT_BOX.md`):
  messages in one lane rotate on that lane's own interval, one at a
  time, in order; different lanes run independently. Lanes are just
  names typed on the page.
- A **global minimum gap** between any two rotated messages, whatever
  lane they came from, so two lanes never post back to back.
- Per message: text, lane, enabled. Per lane: interval in minutes. A
  live-only switch (default on: nothing posts while offline; the stream
  status plugin's `get_status()["is_live"]` is the source). Optional
  "only if chat has had N messages since the last post" is a common
  MixItUp option; ask James whether he wants it before building it.
- Persist to `data/timed_messages.json` via `core/atomic_io`; a new
  `plugins/timed_messages.py` owns the loop and posts with
  `bot.send_chat`; feature toggle `timed_messages` in `DEFAULT_FEATURES`;
  a self-running test in `tests/test_timed_messages.py` (fake bot,
  fake clock, no network) covering lane rotation order, the global gap,
  live-only, and enable/disable. Document in HATMASBOT.md (file
  structure, commands/feature section, a v2.18 note) and the mod page
  help text. Keep the "someday other channels" rule: nothing hardcoded
  to hatmaster, config over literals.

## Task 2: brainstorm "make Hats matter" (talk first, no code yet)

Facts to open with: the 2026-09-04 usage audit found the market is a
passive airdrop (48 buys in 5 months, most portfolios come from free
shares and YouTube comment grants); Hats were invisible to most viewers
because they lived in MixItUp; now every viewer earns 5/min, can see the
balance with `!hats`, and YouTube logins hold Hats too. James wants to
brainstorm sinks and loops together before anything is built. Bring the
candidates that already have half the plumbing: bingo card prices,
priority god requests paid in Hats alongside the $5 Stripe path
(`plugins/priority_request.py`, `plugins/godrequest.py` God Tokens),
a Hats leaderboard on the site next to the portfolio one
(`wallet.leaderboard()` exists), buying God Tokens with Hats, wagers on
the live match (`/live` page and `_LIVE_EVENTS` exist), sub multiplier
and event bonuses (`WALLET_EARN_SUB_MULTIPLIER`, `WALLET_BONUS_*`, all
wired but 0). Ask what he wants viewers to *feel* when they spend Hats
before ranking anything, then write the outcome to a short plan doc the
way `docs/WALLET_PLAN.md` was done (tables with every field, approved
before code).

Deliver Task 1 in full, then open Task 2 as a conversation.
