# 7-6-2026 — Launch TODO for Hatmaster.tv

Everything left before sharing hatmaster.tv with viewers. Written at the
end of the 7/6 session; the state notes below were verified live that
evening. Work through top to bottom — the list is ordered by
launch-blocking first, polish later.

## Verified DONE as of 7/6 (don't redo)

- Offline fallback Worker deployed — bot down now shows the branded
  "Market closed" page instead of Cloudflare error 1033.
- Auto-start installed — "HatmasBot" scheduled task runs run_bot.bat
  (supervisor.py restart loop) at logon. Undo:
  `Unregister-ScheduledTask -TaskName "HatmasBot" -Confirm:$false`
- Web login + trading LIVE — `WEB_TRADING_ENABLED = True` in
  config_local.py; James logged in and traded Geb shares on prod.
- Buy-all / sell-all + "YOU OWN" display shipped (v2.7.2 section in
  HATMASBOT.md). Tests: test_web_trade.py 24/24.
- Cloudflare Browser Cache TTL set to "Respect Existing Headers" —
  auth.js verified serving `Cache-Control: no-cache` through the edge.

## 1. Commit today's work (5 min, do first)

Uncommitted on main: public_webserver.py, auth.js, god.html,
portfolio.html, theme.css, dev_stub_site.py, test_web_trade.py,
HATMASBOT.md (v2.7.2 section), .claude/launch.json. Also untracked
resolve_tiktok.* / make_blur_bg.py / tiktok_preset.json (separate
commit — DaVinci tooling, unrelated).

## 2. Stripe live mode (the big remaining blocker, ~30 min at PC)

Code is production-ready (18 regression tests, two-phase webhook,
reconcile CLI). Only config stands between test and live:

1. Stripe dashboard: activate the account (business details + bank
   for payouts).
2. Developers → Webhooks → Add endpoint
   `https://hatmaster.tv/api/stripe-webhook`, events:
   `checkout.session.completed`, `charge.refunded`,
   `charge.dispute.created`.
3. config_local.py: swap `sk_test_...` → `sk_live_...` and the test
   `whsec_` → the live endpoint's signing secret. Restart bot.
4. Self-test: pay the real $5 with your own card on /community, watch
   the god hit the queue head + chat announcement, then refund via
   `python tools/reconcile_stripe.py refund <session_id> --yes`.
5. Adopt end-of-stream ritual: `reconcile_stripe.py audit` +
   `unplayed` (refund candidates). Known limitation: if the bot is
   down when someone pays, Stripe retries ~3 days and it self-heals
   on next launch — only advertise priority requests while live.

## 3. Refund policy + terms blurb on /community (30 min)

Verified absent (zero refund/terms mentions in community.html). Before
taking real money, the $5 priority card needs 2–3 plain sentences:
what you're buying, "if your god never gets played, you get refunded",
and a contact (Twitch whisper / email). Matches tooling that already
exists (`reconcile_stripe.py unplayed`). Tone rule applies: plain
text, no emojis.

## 4. Live-fire trading checks (one stream's worth of attention)

Web trading is enabled but has only had James-testing. During the next
stream, watch for:
- A viewer (not the broadcaster) logging in + trading from a phone.
- Cooldown + rate-limit messages reading sensibly to a stranger.
- Trade feed overlay showing web trades correctly on stream.
- Optional hardening (design doc going-live step 4): Cloudflare WAF
  rate rule on `/api/trade` + `/auth/*` (e.g. 60 req/min/IP) for edge
  defense in depth. Dashboard-only, no code.

## 5. Fresh-eyes content pass on the site (1–2 hrs)

Pages are solid (landing/portfolio/god/community reviewed 7/6 — hero,
OG cards, market explainer all good). Remaining polish:
- Walk every page logged OUT on a phone — the first-visit experience
  viewers actually get. Check the login prompt sells itself on god
  pages ("Log in with Twitch to buy and sell shares").
- /community: check queue + poll render sensibly when empty/offline.
- 404 page, /priority-success page — read once end to end.
- Leaderboard: confirm bot accounts are excluded and names render.

## 6. Uptime odds and ends (low effort, do whenever)

- Reboot test: restart the PC once, confirm bot auto-launches at
  logon and hatmaster.tv comes back without touching anything.
  (Task Scheduler quirk: starts at LOGON, so a reboot to the lock
  screen leaves the site on the offline page until sign-in — fine.)
- `cloudflared` Windows service: `sc query cloudflared` says RUNNING
  and startup type auto. If bot runs its own tunnel child too, that's
  harmless (replica) but set `CLOUDFLARED_ENABLED = False` in
  config_local.py to stop the redundancy — HATMASBOT.md Cloudflare
  section explains.
- Old `HatmasBot` tunnel (previous PC) still exists in the Cloudflare
  dashboard pending deletion — don't confuse with live `HatBot`.

## 7. Announcement-day sanity list (morning-of)

- `python tools/check_stream_ready.py` (includes web-login probe).
- `python tools/test_web_trade.py` + `test_web_session.py` +
  `test_priority_request.py` all green.
- Browse hatmaster.tv logged out from a phone on cellular (not home
  wifi) — landing, a god page, a portfolio, /community.
- Buy + sell 1 share from the phone.
- Kill the bot once, confirm the offline page, restart.

## Explicitly deferred (decided 7/6, don't re-litigate)

- Cloud/always-on hosting: NOT pursuing. PC-hosted + tunnel + offline
  Worker + supervisor + logon task is the launch architecture. 24/7
  read-only mirror is a phase-2 idea only if viewers ask.
- FindIt stays behind its default-OFF toggle for launch.
- Versioned asset URLs (`/auth.js?v=N`): unnecessary now that
  Cloudflare respects origin cache headers; revisit only if stale-JS
  reports come back.
